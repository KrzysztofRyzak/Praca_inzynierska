<!DOCTYPE html>
<html lang="pl">
<head>
    <%- include("../partials/head", { title: "Usuń lekcję" }) %>
</head>
<body>
    <%- include("../partials/navbar", { user }) %>

    <div class="container mt-5">
        <h2 class="text-center text-danger fw-bold"> Usuń lekcję</h2>
        <p class="text-muted text-center">Czy na pewno chcesz usunąć tę lekcję? Tej operacji nie można cofnąć.</p>

        <div class="card mx-auto mt-4 shadow-sm border-danger" style="max-width: 600px;">
            <div class="card-body text-center">
                <h4 class="text-danger fw-bold"><%= lesson.title %></h4>
                <p><strong>Kategoria:</strong> <span class="badge bg-primary"><%= lesson.category %></span></p>
                <p><strong>Poziom trudności:</strong> <span class="badge bg-warning text-dark"><%= lesson.difficulty %></span></p>
                <p class="text-muted"><%= lesson.description %></p>

                <!-- Formularz usuwania lekcji -->
                <form action="/lessons/delete/<%= lesson.id %>" method="post" class="mt-3">
                    <button type="submit" class="btn btn-danger w-100 fw-bold"> Tak, usuń lekcję</button>
                </form>
            </div>
        </div>

        <!-- Przycisk cofania -->
        <div class="text-center mt-4">
            <a href="/lessons" class="btn btn-secondary fw-bold"> Anuluj</a>
        </div>
    </div>

    <%- include("../partials/footer") %>
</body>
</html>
