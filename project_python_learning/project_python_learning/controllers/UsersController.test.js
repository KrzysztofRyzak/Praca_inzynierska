describe("UsersControllerTest", () => {
    test('getAll should fetch many', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getAllUsersByRole("teacher") should find many', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('createUser should return created user', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('validPassword correct password should return true', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('validPassword not correct password should return false', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById should return user', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById user not exists should return empty', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('updateById should return updated user', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getUserByEmail user with email exists should return user', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getUserByEmail user with email not exists should return empty', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('deleteById should remove user', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });
});