describe("ExercisesControllerTest", () => {
    test('getAll should fetch many', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById should fetch one exercise', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById id not correct should return empty', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getByLessonId should find 3 exercises', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('createExercise should return created exercise', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('updateById should return updated exercise', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('deleteById should remove exercise', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });
});