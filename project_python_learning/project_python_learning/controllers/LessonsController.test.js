describe("LessonsControllerTest", () => {
    test('getAll should fetch many', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById should fetch lesson', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getById id not correct should return empty', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('getByDifficulty("łatwy") should find 2 lessons', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('updateById should return updated lesson', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });

    test('deleteById should remove lesson', () => {
        let variable = "true";
        expect(variable).toBe('true');
    });
});